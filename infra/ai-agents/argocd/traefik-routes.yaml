# Deploys Traefik IngressRoutes for all services exposed via Cloudflare Tunnel hostnames.
# Picks up all *.yaml files in infra/ai-agents/traefik/ — add new IngressRoute files there.
# Deploys to the same clusters as traefik (cluster-role=ai-agents).
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: traefik-routes
  namespace: argocd
spec:
  generators:
    - clusters:
        selector:
          matchLabels:
            cluster-role: ai-agents
  template:
    metadata:
      name: "traefik-routes-{{name}}"
    spec:
      project: default
      source:
        repoURL: https://github.com/kylep/multi
        targetRevision: main
        path: infra/ai-agents/traefik
        directory:
          include: "*.yaml"
      destination:
        server: "{{server}}"
        namespace: argocd
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
