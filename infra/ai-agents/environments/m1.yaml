# M1 — always-on server ("Pai's laptop"). Runs all scheduled agents and pai-responder.
storageClass: local-path

# pai-responder: enabled on M1 (primary Discord listener)
enabled: true

tasks:
  journalist:
    enabled: true
  paiMorning:
    enabled: true
  publisher:
    enabled: false
  analyst:
    enabled: false
