# M1 — always-on server ("Pai's laptop"). Runs all scheduled agents and pai-responder.
storageClass: local-path

paiResponder:
  enabled: true

cronjobs:
  journalist:
    enabled: true
    schedule: "0 12 * * *"   # 8am ET
  paiMorning:
    enabled: true
    schedule: "30 12 * * *"  # 8:30am ET
