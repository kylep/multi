# M1 — always-on server ("Pai's laptop"). Runs all scheduled agents and pai-responder.
storageClass: local-path

paiResponder:
  enabled: true

tasks:
  journalist:
    enabled: true
  paiMorning:
    enabled: true
