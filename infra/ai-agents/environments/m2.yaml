# M2 — gaming machine, Rancher sometimes off. No scheduled agents; available for ad-hoc testing.
storageClass: local-path

paiResponder:
  enabled: false  # avoid duplicate Discord responses; M2 unreliable

tasks:
  journalist:
    enabled: false
  paiMorning:
    enabled: false
  publisher:
    enabled: false
  analyst:
    enabled: false
