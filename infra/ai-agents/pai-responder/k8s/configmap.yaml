# ConfigMaps are generated by kustomization.yaml's configMapGenerator.
# To apply manually without kustomize:
#
# kubectl create configmap pai-responder-script \
#   --from-file=poll-loop.py=infra/ai-agents/pai-responder/poll-loop.py \
#   --namespace=ai-agents --dry-run=client -o yaml | kubectl apply -f -
#
# kubectl create configmap pai-discord-mcp \
#   --from-file=server.py=apps/mcp-servers/discord/server.py \
#   --namespace=ai-agents --dry-run=client -o yaml | kubectl apply -f -
