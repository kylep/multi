{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pai-responder.fullname" . }}-script
  labels:
    {{- include "pai-responder.labels" . | nindent 4 }}
data:
  poll-loop.py: |
{{ .Files.Get "files/poll-loop.py" | indent 4 }}
  discord-mcp-server.py: |
{{ .Files.Get "files/discord-mcp-server.py" | indent 4 }}
{{- end }}
