{{- if .Values.cronjobs.tweetRss }}
{{- if .Values.cronjobs.tweetRss.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tweet-rss-state
  namespace: ai-agents
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Mi
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: tweet-rss
  namespace: ai-agents
spec:
  schedule: {{ .Values.cronjobs.tweetRss.schedule | quote }}
  timeZone: "UTC"
  startingDeadlineSeconds: 900
  suspend: false
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      backoffLimit: 0
      activeDeadlineSeconds: 120
      ttlSecondsAfterFinished: 3600
      template:
        metadata:
          annotations:
            vault.hashicorp.com/agent-inject: "true"
            vault.hashicorp.com/role: "ai-agents"
            vault.hashicorp.com/agent-inject-secret-config: "secret/ai-agents/twitter"
            vault.hashicorp.com/agent-inject-template-config: |
              {{`{{- with secret "secret/ai-agents/twitter" -}}`}}
              export TWITTER_API_KEY="{{`{{ .Data.data.api_key }}`}}"
              export TWITTER_API_KEY_SECRET="{{`{{ .Data.data.api_key_secret }}`}}"
              export TWITTER_ACCESS_TOKEN="{{`{{ .Data.data.access_token }}`}}"
              export TWITTER_ACCESS_TOKEN_SECRET="{{`{{ .Data.data.access_token_secret }}`}}"
              {{`{{- end -}}`}}
        spec:
          serviceAccountName: cronjob-agent
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            fsGroup: 1001
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: tweet-rss
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command: ["/bin/sh", "-c"]
              args:
                - |
                  . /vault/secrets/config
                  pip3 install --break-system-packages -q requests requests-oauthlib 2>/dev/null
                  git clone --depth 1 https://github.com/kylep/multi.git /workspace/repo 2>/dev/null
                  python3 /workspace/repo/infra/ai-agents/cronjobs/scripts/tweet-rss.py
              resources:
                requests:
                  cpu: 50m
                  memory: 64Mi
                limits:
                  cpu: 200m
                  memory: 256Mi
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
              volumeMounts:
                - name: cache
                  mountPath: /cache
                - name: workspace
                  mountPath: /workspace
          volumes:
            - name: cache
              persistentVolumeClaim:
                claimName: tweet-rss-state
            - name: workspace
              emptyDir: {}
{{- end }}
{{- end }}
