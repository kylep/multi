apiVersion: agents.kyle.pericak.com/v1alpha1
kind: AgentTask
metadata:
  name: publisher-manual
  namespace: ai-agents
spec:
  agent: publisher
  runtime: claude
  prompt: "Write a blog post about <REPLACE WITH TOPIC>"
  trigger: manual
  readOnly: false
