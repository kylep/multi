service:
  type: ClusterIP   # No LoadBalancer — cloudflared talks directly to the pod

ports:
  web:
    port: 80
    expose:
      default: true

# Disable dashboard — internal cluster only
ingressRoute:
  dashboard:
    enabled: false
