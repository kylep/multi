{{- if .Values.tasks.analyst.enabled }}
apiVersion: agents.kyle.pericak.com/v1alpha1
kind: AgentTask
metadata:
  name: manual-analyst
  namespace: ai-agents
spec:
  agent: analyst
  runtime: claude
  prompt: "Review the wiki pages and suggest improvements"
  trigger: manual
  readOnly: true
{{- end }}
