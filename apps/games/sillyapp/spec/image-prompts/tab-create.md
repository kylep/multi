# Tab Bar Icon — Create

**Filename:** `tab-create.png`  
**Size:** 60×60  
**Format:** PNG with transparency (will be used as template image)

## Prompt

A simple, clean tab bar icon representing content creation. A circle with a plus sign (+) in the center, rendered as a single-color silhouette in medium gray (#8E8E93). Thick consistent stroke weight suitable for iOS tab bar at 60x60 pixels. The plus is bold and centered within the circle. Transparent background. Centered. Designed to work as an iOS template image (single color, system will apply tint).
